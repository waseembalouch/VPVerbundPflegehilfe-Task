# =============================================================================
# Docker Ignore File
# =============================================================================
# Prevents unnecessary files from being copied into the Docker image
# This keeps the image size small and build times fast
# =============================================================================

# Dependencies
# -----------------------------------------------------------------------------
# node_modules will be installed fresh in the container
node_modules
package-lock.json.backup
yarn.lock
pnpm-lock.yaml

# Build Outputs
# -----------------------------------------------------------------------------
# Dist folder is built inside the container
dist
build
.cache

# Testing
# -----------------------------------------------------------------------------
# Exclude test files and test coverage
src/test
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
coverage
.nyc_output

# Environment Files
# -----------------------------------------------------------------------------
# .env.production is the only one we need (it's explicitly copied)
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production.local

# Version Control
# -----------------------------------------------------------------------------
.git
.gitignore
.gitattributes

# IDE and Editor Files
# -----------------------------------------------------------------------------
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Docker Files
# -----------------------------------------------------------------------------
# Don't copy Docker files into the image
Dockerfile
Dockerfile.*
.dockerignore
docker-compose*.yml

# Documentation
# -----------------------------------------------------------------------------
*.md
docs/
LICENSE

# Logs and Temporary Files
# -----------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.pid
*.seed
*.pid.lock
Thumbs.db

# Misc
# -----------------------------------------------------------------------------
.prettierrc*
.eslintrc*
.editorconfig
